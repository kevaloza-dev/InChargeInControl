const Quiz = require('../models/Quiz');

const initScheduler = () => {
  console.log('Daily Quiz Scheduler Initialized (AI Generation Disabled)...');
  // No active tasks for now as AI generation is removed
};

module.exports = { initScheduler };
